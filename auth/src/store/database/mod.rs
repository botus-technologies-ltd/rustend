// Database store module
// Note: MongoDB store implementations are in separate files
// and require the mongodb feature to be enabled

// For now, we export empty module - store implementations 
// should be added based on the database being used
